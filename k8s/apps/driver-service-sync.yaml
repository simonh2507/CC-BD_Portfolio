apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: driver-service
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure
    - name: mongodb
  interval: 10m
  path: ./k8s/apps/driver-service
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: ride-sharing
